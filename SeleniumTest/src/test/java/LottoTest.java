import org.junit.Assert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

import java.util.List;

/*
public class LottoTest {
    private WebElement driver;
    List<WebElement> listaWebElementow = driver.findElements(By.xpath("//*[text()='12637']/../..//*[@class='scoreline-item circle']"));
    WebElement rodo = driver.findElement(By.cssSelector("[class='orange-btn']"));
rodo.click();
for(int i=0; i<listaWebElementow.size()-1;i++){
        WebElement kula = listaWebElementow.get(i);
        WebElement kulaKolejna = listaWebElementow.get(i+1);
        int wartoscKuli = Integer.parseInt(kula.getText());
        System.out.println("Wartosc pierwszej kuli = " + wartoscKuli);
        int wartoscKuliKolejnej = Integer.parseInt(kulaKolejna.getText());
        System.out.println("Wartosc drugiej kuli = " + wartoscKuliKolejnej);
        Assert.assertTrue(wartoscKuli<wartoscKuliKolejnej);
    }
}
*/
